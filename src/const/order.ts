export enum ORDER_ERROR_ENUM {
  ORDER_NOT_FOUND = 'Order does not exist',
  STATUS_NOT_FOUND = 'Order status does not exist',
  DUPLICATE_STATUS = 'Duplicate order status',
  ORDER_ITEM_NOT_FOUND = 'There is a order item that does not exist',
}

export enum ORDER_MESSAGE_ENUM {
  SUCCESS = 'Success',
}
